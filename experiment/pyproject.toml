[project]
name = "experiment"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "trl-env",
    "mini-racer>=0.14.1",
    "pydantic>=2.13.3",
    "sympy",
]

[tool.uv.sources]
trl-env = { workspace = true }